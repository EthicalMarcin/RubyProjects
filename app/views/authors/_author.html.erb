
 		
<div class = 'container'>
 			<div class="row" data-author-id="<%= author.id %>">
            
            <div class="col-sm-12 col-md-8"><%= link_to author.fullname, author_path(id: author.id) %><hr></div>
            <div class="col-sm-12 col-md-2"><%= link_to 'Edit', edit_author_path(id: author.id),  method: :delete, remote: true, role: 'button', class: 'btn btn-primary' %></div>
            <div class="col-sm-12 col-md-2"><%= link_to 'Destroy', author_path(id: author.id), method: :delete, remote: true, data: { confirm: 'Are you sure?'}, role: 'button', class: 'btn btn-danger' %> <!-- uzyj metody HTTP DELETE --> </div>
        </div>
 </div>

